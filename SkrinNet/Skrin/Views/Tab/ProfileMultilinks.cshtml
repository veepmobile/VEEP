@model Skrin.Models.ProfileMultilinks.ProfileMultilinksModel
@{Layout = null;}
@using Skrin.BLL;
@using Skrin.BLL.Infrastructure
@using Skrin.BLL.Authorization
@using Skrin.BLL.Root
@using Newtonsoft.Json

@{
    UserSession us = Context.GetUserSession();
    string search_onclick = us.AuthenticationResult == AuthenticationType.Ok ? "doSearch('" + @Model.iss + "');" : "united_login(5);";
    //search_onclick = "doSearch('" + @Model.iss + "');";
    int user_id = us.UserId;
}

<link href="~/Content/search.css" rel="stylesheet" />
<link href="~/Content/groups.css" rel="stylesheet" />
<link href="~/Content/multilinks.css" rel="stylesheet" />

<script src="/Scripts/login_dialog.js"></script>
<script src="~/Scripts/search/multilinks.js"></script>
<iframe class="service_frame" name="xls_frame" id="xls_frame" onload="hideClock();"></iframe>

<div>
    <div class="form-group">
        <label for="codes" class="label_form" style="width:100px">Мой список</label>
        <div class="sub_form">
            <textarea id="codes" class="search_input" style="width:800px; height: 90px" placeholder="ИНН, ОГРН, РТС/СКРИН, Ф.И.О."></textarea>
        </div>
    </div>
    <div class="form-group">
        <button id="btn_search" class="btns darkblue large" onclick="@search_onclick">Найти</button>
        <button class="btns grey large" onclick="doClear()">Очистить</button>
    </div>
    <div id="info" style="padding-top: 10px; height: auto">
        1. Загрузить свой список юридических лиц (ЮЛ) и/или физических лиц (ФЛ) можно по следующим кодам: ЮЛ - ИНН, ОГРН, код СКРИН/РТС, ФЛ - ИНН, Ф.И.О.<br />
        2. Разделителем между кодами является запятая или перевод строки<br />
        3. Максимальное количество загружаемых кодов - 100
    </div>
</div>
<div id="multilinks_content">
    <div id="search_result" style="display:none;">
    </div>
</div>
