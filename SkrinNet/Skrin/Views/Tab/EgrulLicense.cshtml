@model Skrin.Models.Iss.Content.LicenseModel
@{
    Layout = null;
}
Данные на @(DateTime.Now.ToRusString())
@if (Model.LicenseTable != null && Model.LicenseTable.RowCount > 0)
{
    @Html.Partial("~/Views/Tab/HorisontalTable.cshtml", @Model.LicenseTable)
    <br />
}
